<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cronometro">

    <application
        android:allowBackup="true"
        android:icon="@drawable/circle"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CRONOMETRO">
        <activity
            android:name=".CRONOMETRO"
            android:label="@string/title_activity_c_r_o_n_o_m_e_t_r_o"
            android:theme="@style/Theme.CRONOMETRO.NoActionBar"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CRONOMETRO.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>